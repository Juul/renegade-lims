'use strict';

var fs = require('fs');

const settings = {
  host: 'localhost',
  port: 4444,
  dataPath: './data',

  // initial credentials for admin user
  admin: {
    username: "admin",
    password: "admin"
  },

  tlsKey: fs.readFileSync('tls/server-key.pem'),
  tlsCert: fs.readFileSync('tls/server-cert.pem'),
  tlsClients: [
    {
      type: 'lab', // lab clients have full access to db
      description: "some computer",
      cert: fs.readFileSync('tls/clients/swabber.localhost.pem')
    },
    {
      type: 'field', // field clients can't access lab inventory
      description: "juul's laptop",
      cert: fs.readFileSync('tls/clients/swabber.localhost.pem')
    },
  ],

  checkTimeEvery: 60 * 60, // how often to check time accuracy (in seconds)
  ntpServers: [
    {host: '0.pool.ntp.org', port: 123},
    {host: '1.pool.ntp.org', port: 123},
    {host: '2.pool.ntp.org', port: 123},
    {host: '3.pool.ntp.org', port: 123}
  ]
  
};

module.exports = settings;
